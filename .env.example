# For local dev (client-side mock API)
VITE_USE_MOCK=1

# For Netlify functions (server-side mock)
# USE_MOCK=1

# Live Claude (set on Netlify, not locally committed)
# CLAUDE_API_KEY=sk-...
# CLAUDE_MODEL=claude-3-5-sonnet-20240620
